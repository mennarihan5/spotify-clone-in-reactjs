import styles from './MainDasboard.module.css';

export const MainDashboard = () => {
    return (
        <section className={styles.container}>
            <h2 className={styles.title}>Good afternoon</h2>
        </section>
    )
}